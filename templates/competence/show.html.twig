{% extends 'base.html.twig' %}

{% block title %}Competence{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ competence.nom }}</h1>

        <ul class="list-group">
            <h2>Etudiants avec la compétence</h2>
            {% for user in competence.users %}
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span class="text-black-50"><a href="{{ path('user_show',{'id':user.id}) }}"
                       class="text-black-50 ">{{ user.fullname }} {{ user.ecole }}</a></span></li>

            {% endfor %}
        </ul>
        <a class="text-danger" href="{{ path('projet_index') }}">back to list</a>


    </div>




{% endblock %}
