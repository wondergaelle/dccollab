<article class="container">
    <h5>{{ competence.nom }}</h5>

    <ul class="list-group">

        {% for user in competence.users %}
            <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span lass="badge badge-primary badge-pill"><a href="{{ path('user_show',{'id':user.id}) }}"
                                                                   class="badge ">{{ user.fullname }} {{ user.ecole }}</a></span></li>

        {% endfor %}
    </ul>



</article>